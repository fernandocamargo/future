import { lazy } from 'react';

export const AboutMe = lazy(() => import('./about-me'));

export const Education = lazy(() => import('./education'));

export const Experience = lazy(() => import('./experience'));

export const Skills = lazy(() => import('./skills'));
