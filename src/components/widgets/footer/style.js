import styled from 'styled-components';

export default component => styled(component)`
  background-color: #cda;
  bottom: 0;
  left: 0;
  position: absolute;
  text-align: center;
  width: 100%;
`;
